package Hafta1.ArrayExercises;

public class ArrayExercise4 {
    public static void main(String[] args) {
/*
        //dizideki max min sayıyı bulmak için


        // arrayi tanımladık
        int[] numbers = {1,9,65,846,849,100,941};

        //max sayısını tanımladık
        int max = numbers[0];

        for (int number : numbers)
        {
            if (number>max){
            max = number;
            }
        }
        System.out.println("max num is " + max
        );
    }
}
/*

 */
        int[] numbers = {5, 8, 84, 651, 6516, 5616, 6416, 564, 6946, 6464};

        int max = numbers[0];

        for (int number : numbers) {
            if (number > max) ;
            max = number;
        }
        System.out.println("max değer " + max);
    }}

